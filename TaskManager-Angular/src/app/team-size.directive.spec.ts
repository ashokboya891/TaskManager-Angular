import { TeamSizeDirective } from './team-size.directive';

describe('TeamSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new TeamSizeDirective();
    expect(directive).toBeTruthy();
  });
});
